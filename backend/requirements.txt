# Core dependencies for Wheel of Fortune backend
# No external dependencies required for the core game logic

# For future API development (optional)
# flask>=2.3.0
# fastapi>=0.100.0
# uvicorn>=0.23.0

# For testing (optional)
# pytest>=7.4.0
# pytest-cov>=4.1.0

# For data validation (optional)
# pydantic>=2.0.0

# Python 3.8+ required
# The game engine uses only Python standard library modules:
# - dataclasses (for models)
# - enum (for wheel results and game states)
# - typing (for type hints)
# - uuid (for unique IDs)
# - json (for data persistence)
# - pathlib (for file handling)
# - random (for puzzle selection) 